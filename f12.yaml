---
- name: Install multiple packages using a list.
  hosts: man
  become: true
  tasks:
    - name: install packages
      package:
         name: "{{ item }}"
         state: present
      loop:
        - nginx
        - git
        - tree
